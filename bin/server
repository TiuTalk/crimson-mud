#!/usr/bin/env ruby
# frozen_string_literal: true

require 'bundler/setup'
require 'mud'

telnet_server = Mud::Network::TelnetServer.new

# Trap signals
%w[INT TERM].each do |signal|
  trap(signal) do
    Thread.new { telnet_server.stop }.join
    exit
  end
end

telnet_server.start
