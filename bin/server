#!/usr/bin/env ruby
# frozen_string_literal: true

require 'bundler/setup'
require 'mud'

server = Mud::Network::Server.instance

# Trap signals
%w[INT TERM].each do |signal|
  trap(signal) do
    Thread.new { server.stop }.join
    exit
  end
end

server.start
